{
  "name": "quota-bar",
  "private": true,
  "version": "0.1.0",
  "description": "Windows taskbar quota bar for Anthropic-compatible Claude proxies",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
